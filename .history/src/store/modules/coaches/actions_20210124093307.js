export default {
  registerCoach(context, data) {
    const coachData = {
      firstName: data.first,
      firstName: data.last,
      firstName: data.last
      firstName: data.last
      firstName: data.last
    };
  }
}