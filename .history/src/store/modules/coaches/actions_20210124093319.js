export default {
  registerCoach(context, data) {
    const coachData = {
      firstName: data.first,
      firstName: data.last,
      description: data.desc
      hourly: data.last
      firstName: data.last
    };
  }
}